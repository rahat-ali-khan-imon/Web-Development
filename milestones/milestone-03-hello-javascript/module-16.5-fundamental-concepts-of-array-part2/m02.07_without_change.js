// 2️⃣ Reverse Without Changing Original Array

let numbers = [1, 2, 3, 4, 5];

let reversed = [...numbers].reverse();

console.log(reversed);
console.log(numbers);

// 🧠 এখানে spread operator ব্যবহার করেছি copy বানানোর জন্য।