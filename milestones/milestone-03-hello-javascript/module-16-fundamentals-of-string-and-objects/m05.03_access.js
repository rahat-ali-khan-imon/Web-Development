// Access Object Properties

let person = {
    name: "Rahim",
    age: 25,
    city: "Dhaka"
};

// Dot Notation (Most Common)
console.log(person.name);
console.log(person.age);

// Bracket Notation
console.log(person["city"]);    // যখন property name dynamic হয় তখন bracket use করি।